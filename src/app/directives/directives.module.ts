import { NgModule } from '@angular/core';
import { ClosingAutocompliteDirective } from './closing-autocomplite.directive';

@NgModule({
    declarations: [
      // directive
      ClosingAutocompliteDirective
    ],
    exports: [
      // directive
      ClosingAutocompliteDirective
    ],

  })
  export class DirectivesModule {}
